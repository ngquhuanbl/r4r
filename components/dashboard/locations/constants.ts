/** Primary actions / brand accent (use with inline styles only when Tailwind classes are not applicable) */
export const DASHBOARD_ACCENT = "#007AFF";

/** Dashboard primary CTA — `text-white` (not `text-primary-foreground`) so contrast stays correct on blue in both themes */
export const DASHBOARD_PRIMARY_BUTTON_CLASSNAME =
  "bg-[#007AFF] text-white shadow hover:bg-[#0066D0] dark:bg-[#0A84FF] dark:hover:bg-[#409CFF]";
