/**
 * Google Places–backed address search is disabled until product re-enables it.
 * Keep create and edit dialogs in sync with this flag.
 */
export const ADDRESS_SEARCH_TEMPORARILY_DISABLED = true;
